{
  "name": "@xurizaemon/11ty-passthrucopy",
  "version": "1.0.0",
  "description": "Profiling passthru copy.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "chmod +x ./bin/*.js",
    "lint": "prettier --check 'src/**/*.js'",
    "style": "prettier -w 'src/**/*.js'"
  },
  "author": "Chris Burgess <chris@giantrobot.co.nz>",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.0",
    "prettier": "^3.5.3"
  }
}
